import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import { Example } from './example.stories';
import { ImportPath } from '../../storybookUtil/ImportPath';

<Meta title="New Hook/useMemoRef" component={Example} />

# useMemoRef

#### Example

<Canvas>
  <Story story={Example} inline />
</Canvas>

## Reference

```ts
function useMemoRef<MemoValue, Deps extends DependencyList>(
  factory: () => MemoValue,
  deps: Deps
): MemoValue;
```

#### Importing

<ImportPath />

#### Arguments

- **factory** _`() => unknown`_ - useMemo factory function.
- **deps** _`DependencyList`_ - useMemo dependency list.
